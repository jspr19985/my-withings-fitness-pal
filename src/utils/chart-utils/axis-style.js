const fontFamily = "Major Mono Display";
const fontSize = 10;
const stroke = "white";
const label = { stroke, fontSize, fontFamily };

export const axisStyle = {
    axis: { stroke },
    axisLabel: label,
    ticks: { stroke },
    tickLabels: label
};
